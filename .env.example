# Copy to .env.local and adjust as needed (both are gitignored).

# Kill switch for the interactive map view. Set to false to disable it without
# touching src/app.jsx. Defaults to true when unset.
VITE_ENABLE_MAP=true
